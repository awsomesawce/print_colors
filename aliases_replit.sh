#!/usr/bin/env bash
# Aliases for working in repl.it shell.

alias ls='ls --color=auto'
alias ll='ls -l'
alias la='ls -A'
alias l='ls -CF'

alias dr='deno run'
alias dra="deno run -A"
alias drc="deno check"
